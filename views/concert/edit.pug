extends ../layouts/layout

block content
  .container
    br

    -data = data || {}
    
    h2=title

    if errors
      ul
        for error in errors
          li= error.msg
    
    include form